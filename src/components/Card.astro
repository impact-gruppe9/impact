---
const { title, bgimage } = Astro.props;
---

<article style=`background-image: url(img/${bgimage})`>
  <h2>{title}</h2>
</article>

<style>
  article {
    display: flex;
    justify-content: center;
    align-items: center;

    background-size: cover;
  }
</style>
